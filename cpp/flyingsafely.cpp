#include <iostream>

using namespace std;

int main() {
    int cases;
    cin >> cases;

    for (int t = 0; t < cases; ++t) {
        int n, m;
        cin >> n >> m;

        for (int i = 0; i < m; ++i) {
            int a, b;
            cin >> a >> b;
        }
        cout << n - 1 << endl;
    }
}