#include <bits/stdc++.h>

using namespace std;
using ll = long long;

int main() {
    double p;
    cin >> p;
    cout << setprecision(10) << fixed << (100.0 / p) << endl << (100.0 / (100.0 - p)) << endl;
}
