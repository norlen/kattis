#include <iostream>
#include <inttypes.h>

using namespace std;

int main() {
    int p;
    cin >> p;

    for (int i = 0; i < p; ++i) {
        uint32_t k, b, n;
        cin >> k >> b >> n;

        
    }
}